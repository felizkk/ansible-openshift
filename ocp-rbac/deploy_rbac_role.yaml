---
- name: Test AAP to OCP Playbook
  hosts: localhost
  gather_facts: false

  vars:
    role_name: vm-editor-role
    roleref_name: kubevirt.io:edit
    namespace_name: vm-test-project-1
    user_group_name: vm-editor-group
  
  tasks:
    - name: Create RoleBinding for KubeVirt
      redhat.openshift.k8s:
        state: present
        definition:
          kind: RoleBinding
          apiVersion: rbac.authorization.k8s.io/v1
          metadata:
            name: "{{ role_name }}-{{ namespace_name }}"
            namespace: "{{ namespace_name }}"
          subjects:
            - kind: Group
              apiGroup: rbac.authorization.k8s.io
              name: "{{ user_group_name }}"
          roleRef:
            apiGroup: rbac.authorization.k8s.io
            kind: ClusterRole
            name: "{{ roleref_name }}"

    - name: Create RoleBinding for namespace view
      redhat.openshift.k8s:
        state: present
        definition:
          apiVersion: rbac.authorization.k8s.io/v1
          kind: RoleBinding
          metadata:
            name: "{{ role_name }}-{{ namespace_name }}-ns-view"
            namespace: "{{ namespace_name }}"
          subjects:
            - kind: Group
              apiGroup: rbac.authorization.k8s.io
              name: "{{ user_group_name }}"
          roleRef:
            apiGroup: rbac.authorization.k8s.io
            kind: ClusterRole
            name: view
